#include <stdio.h>
main()
{
	char str[100];
	int i;
	printf("char %d  f %d  double %d\n",sizeof(char),sizeof(float),sizeof(double));
	/*printf("Input the book name: ");
	gets(str);
	for(i=0;i<=strlen(str);i++)
		if(str[i]>='a'&&str[i]<='z')
			str[i]-=32;
	puts(str);*/
}
